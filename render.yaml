services:
  - type: web
    name: secure-tunnel-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m secure_tunnel.server.tunnel_server
    healthCheckPath: /
    envVars:
      - key: TUNNEL_SECRET
        sync: false   # à définir manuellement dans le dashboard Render
